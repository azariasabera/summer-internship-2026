# conf/asr/asr.yaml

asr_model: openai/whisper-large-v3
batch_size: 8
language: fi
use_precomputed: false # Whether to run Whisper or to add the tranlation and transcription columns directly from the already prepared document.

annotation_csv_dir: ${paths.annotation_root}
annotation_json_dir: ${paths.chunk_meta_dir}

audio_root: ${paths.audio_root}

prepared_annotation_root: ${paths.prepared_annotation_root}